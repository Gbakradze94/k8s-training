apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "song-chart.fullname" . }}-test-connection"
  labels:
    {{- include "song-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "song-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

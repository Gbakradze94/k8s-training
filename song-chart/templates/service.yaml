apiVersion: v1
kind: Service
metadata:
  name: {{ include "song-chart.fullname" . }}
  labels:
    {{- include "song-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "song-chart.selectorLabels" . | nindent 4 }}

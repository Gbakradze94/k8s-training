server:
  port: 8081
spring:
  application:
    name: song-service
  # datasource configuration
  datasource:
    url: jdbc:postgresql://localhost:5432/song_service_db
    username: user
    password: password
    driverClassName: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.PostgreSQLDialect
#eureka:
#  client:
#    serviceUrl:
#      defaultZone: http://localhost:8083/eureka


#
#song-service:
#  path: /api/v1/songs